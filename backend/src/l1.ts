/**
 * DEPRECATED — L1 (longlist gate) removed in Phase 0.
 *
 * Cases now arrive pre-filtered by human Round 1, so the platform performs L2 only.
 * This file is kept as an empty stub to avoid breaking TypeScript paths that may still
 * resolve old import paths during the cleanup transition. Do not import from here.
 *
 * See IMPLEMENTATION_PLAN.md and TK_UPDATE_ADDENDUM.md for the new architecture.
 */
export {};
