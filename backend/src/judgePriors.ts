/**
 * DEPRECATED — Judge prior tags (NO_BASELINE, NO_CAUSALITY, etc.) removed in Phase 0.
 *
 * These were derived from historical judge comments and used to bias L2 scoring toward
 * past human concerns. Under the finalized architecture, scoring is driven by the
 * regulation + anchored rubric, not by inferred priors from previous years.
 * Retained as an empty stub to keep import paths resolvable during cleanup.
 */
export {};
